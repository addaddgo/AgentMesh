# UI 设计
- tool panel 框头是绿色，而内部背景还是灰白色（查看 todo panel）。
- list 边框颜色是深灰色
- 后端代码改后，要重启后端服务。
- 添加的 tool，需要在 add dialog 中的 tool tab 添加一个项。tool 以单一面板出现, 其 UI 设计参考 todo panel。
# 记忆指令
- 当用户提到"记忆"时，将用户希望记住的内容写入此文件。
